export default ({
  _clinextType: "question",
  name: 'token',
  type: 'string',
  promptType: 'password',
  alias: 'u',
  message: 'Token',
  // validators: [{
  //   regex: "^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{8,}$"
  // }]
})
